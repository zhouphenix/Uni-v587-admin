import "./common"

const install = (Vue, options = {}) => {
	let $v587 = {
		author: "phenix",
		version: "1.0.0"
	}
	
	Vue.prototype.$v587 = uni.$v587 = $v587
	console.log('v587-ui 插件加载成功 ');
}

export default {
	install
}